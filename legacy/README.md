# Retired Streamlit interface

`streamlit_app.py` is the final pre-retirement implementation retained only as a
short-term rollback/reference artifact. Streamlit is no longer an installed runtime,
an active interface, or a supported daily-run command.

The supported application is the React production build served by FastAPI. Do not add
new product behavior to the legacy file.
